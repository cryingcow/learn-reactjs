import React from 'react';
import PropTypes from 'prop-types';
import { Box } from '@mui/system';

Ads.propTypes = {
    
};

function Ads(props) {
    return (
        <Box>
            
        </Box>
    );
}

export default Ads;